Please avoid changing signatures and names of existing functions. It breaks backwards 
compatibility with existing code, i.e., in the user guide, assignments, etc. 

New library functions are okay to introduce, including those that happen to be duplicating functionality. We should then go through the discussion of deprecation of existing libraries.
