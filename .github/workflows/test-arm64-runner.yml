# .github/workflows/test-arm64-runner.yml
name: Test arm64 runner availability
on: workflow_dispatch  # manual trigger only
jobs:
  test-arm64:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: uname -m  # should print "aarch64"

