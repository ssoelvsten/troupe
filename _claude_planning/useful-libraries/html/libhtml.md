Create a new library called Html for generating HTML documents. Include useful combinators, for constructing various html components. Check out the Time library for the idea of representing various types as records. You may go a bit further and create aux functions instead of immediate record construction.

