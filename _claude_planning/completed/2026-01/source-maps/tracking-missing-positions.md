We have extended the pretty printing in the compiler to now include source positions throughout the intermediate representation, using `--debug-pp` flag, that must be used in combination with `-v` flag. Start running the golden tests. When there is a discrepancy, analyze it. If the source information is incorrect, use that flag in debugging the source position issues. Whenever there is an issue in the files note that _some_ prior positions in the .golden files were incorrect and you should analyze that carefully.