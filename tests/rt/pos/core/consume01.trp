let fun f () = 
      (* note that consume returns both the data and the meta-data *)
      let val x = consume (0, `{}`, `{}`) 
          val y = consume (0, `{}`, `{}`)
      in print (x,y) 
      end 
    val p = spawn f 
in send (p, 1); sleep (200); send (p,2)
end 
